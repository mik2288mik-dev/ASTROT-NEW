# 🚀 Railway Deployment Guide

## ✅ Что настроено

### 1. Драйвер базы данных
- ✅ `pg` (PostgreSQL) добавлен в `package.json` dependencies
- ✅ `@types/pg` добавлен для TypeScript типов
- ✅ Реальное подключение к Railway Database настроено в `lib/db.ts`

### 2. Автоматические миграции
- ✅ Система миграций создана (`lib/migrations.ts`)
- ✅ Миграции запускаются автоматически при деплое через `postbuild` скрипт
- ✅ API endpoint для ручного запуска: `/api/migrations/run`
- ✅ Скрипт для ручного запуска: `npm run migrate`

### 3. Структура базы данных
Миграции создают следующие таблицы:

**users** - Профили пользователей
- id (VARCHAR PRIMARY KEY)
- name, birth_date, birth_time, birth_place
- is_setup, language, theme
- is_premium, is_admin
- three_keys (JSONB), evolution (JSONB)
- premium_activated_at, premium_stars_amount, premium_transaction_id
- created_at, updated_at

**charts** - Натальные карты пользователей
- user_id (VARCHAR PRIMARY KEY, FOREIGN KEY)
- chart_data (JSONB)
- created_at, updated_at

**migrations** - Отслеживание примененных миграций
- id, name, applied_at

## 🔧 Как работает деплой на Railway

### Автоматический процесс:

1. **Build** (`npm run build`)
   - Next.js собирает приложение
   - Проверяет типы TypeScript
   - Оптимизирует код

2. **Postbuild** (`npm run migrate`)
   - Автоматически запускается после успешной сборки
   - Подключается к Railway Database
   - Применяет все миграции в порядке создания
   - Пропускает уже примененные миграции

3. **Start** (`npm start`)
   - Запускает Next.js production сервер
   - Приложение готово к работе

### Логи Railway

При деплое вы увидите:
```
[Migrations] Starting database migrations...
[Migrations] Database connection established
[Migrations] Migrations table created/verified
[Migrations] Applying migration 001_create_users_table...
[Migrations] Migration 001_create_users_table applied successfully
[Migrations] Applying migration 002_create_charts_table...
[Migrations] Migration 002_create_charts_table applied successfully
[Migrations] Applying migration 003_create_indexes...
[Migrations] Migration 003_create_indexes applied successfully
[Migrations] All migrations completed successfully
```

## 📋 Требования для деплоя

### Переменные окружения в Railway

Убедитесь что установлены:
- ✅ `DATABASE_URL` - Connection string от Railway Database (автоматически добавляется при подключении БД)
- ✅ `NODE_ENV=production` - Устанавливается автоматически Railway
- Опционально: `OPENAI_API_KEY`, `EPHE_PATH`, `USE_SWE_WASM`

### Railway Database

1. Создайте PostgreSQL database в Railway
2. Подключите его к вашему проекту
3. Railway автоматически добавит `DATABASE_URL` в переменные окружения

## 🧪 Проверка после деплоя

### 1. Проверьте логи Railway

В Railway Dashboard → Deployments → Logs:
- Должны быть логи миграций
- Не должно быть ошибок подключения к БД
- Приложение должно успешно стартовать

### 2. Проверьте API endpoints

```bash
# Проверка здоровья API
curl https://your-app.railway.app/api/users/current

# Ручной запуск миграций (если нужно)
curl -X POST https://your-app.railway.app/api/migrations/run
```

### 3. Проверьте базу данных

В Railway Database → Query:
```sql
-- Проверка таблиц
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public';

-- Проверка миграций
SELECT * FROM migrations ORDER BY applied_at DESC;

-- Проверка пользователей
SELECT COUNT(*) FROM users;
```

## 🔄 Добавление новых миграций

1. Откройте `lib/migrations.ts`
2. Создайте новую функцию миграции:
```typescript
async function migration004(pool: Pool): Promise<void> {
  const migrationName = '004_your_migration';
  if (await isMigrationApplied(pool, migrationName)) return;
  
  log.info(`Applying migration ${migrationName}...`);
  await pool.query('YOUR SQL HERE');
  await markMigrationApplied(pool, migrationName);
}
```

3. Добавьте вызов в `runMigrations()`:
```typescript
await migration004(pool);
```

4. При следующем деплое миграция применится автоматически

## 🐛 Troubleshooting

### Миграции не запускаются

**Проблема**: В логах нет записей о миграциях

**Решение**:
1. Проверьте что `DATABASE_URL` установлен в Railway
2. Запустите миграции вручную через API: `POST /api/migrations/run`
3. Проверьте логи Railway на наличие ошибок

### Ошибка подключения к БД

**Проблема**: `Error: connect ECONNREFUSED` или подобное

**Решение**:
1. Убедитесь что Railway Database создан и подключен
2. Проверьте `DATABASE_URL` в переменных окружения
3. Убедитесь что драйвер `pg` установлен (должен быть в `package.json`)

### Таблицы не создаются

**Проблема**: Миграции пропускаются, но таблиц нет

**Решение**:
1. Проверьте таблицу `migrations`: `SELECT * FROM migrations;`
2. Если миграции помечены как примененные, но таблиц нет - удалите записи и перезапустите
3. Или создайте таблицы вручную через SQL

### Приложение не стартует

**Проблема**: Railway не может запустить приложение

**Решение**:
1. Проверьте что `package.json` содержит правильные скрипты
2. Убедитесь что `npm run build` проходит успешно локально
3. Проверьте логи Railway на ошибки сборки

## 📚 Дополнительная документация

- `MIGRATIONS.md` - Подробное руководство по миграциям
- `README.md` - Общая документация проекта
- `CHECKLIST.md` - Чеклист выполненной работы

## ✅ Готово к деплою!

Все настроено и готово к работе. При деплое на Railway:
1. ✅ Драйвер БД установится автоматически
2. ✅ Миграции запустятся автоматически после сборки
3. ✅ Таблицы создадутся автоматически
4. ✅ Приложение подключится к базе данных

**Просто сделайте мердж и Railway сделает все остальное!** 🚀
