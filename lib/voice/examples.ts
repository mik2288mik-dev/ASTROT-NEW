export function getPersonalForecastExamples(
  period: 'day' | 'week' | 'month',
  language: 'ru' | 'en' = 'ru'
): string {
  return '';
}

export function getNatalStoryExamples(language: 'ru' | 'en' = 'ru'): string {
  return '';
}

export function getNatalAstrologyExamples(language: 'ru' | 'en' = 'ru'): string {
  return '';
}
