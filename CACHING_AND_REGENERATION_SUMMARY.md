# Система кэширования и регенерации

## ✅ Полностью проверено и работает

### 1. Кэширование всех генераций

**Место хранения:** `UserProfile.generatedContent`

#### Что кэшируется:
- ✅ **Three Keys** (три ключа) - генерируются 1 раз
  - Энергия (key1)
  - Стиль любви (key2)
  - Карьера (key3)
  
- ✅ **Deep Dive анализы** - генерируются 1 раз
  - Личность (personality)
  - Любовь (love)
  - Карьера (career)
  - Слабости (weakness)
  - Карма (karma)

- ✅ **Гороскопы** - обновляются по расписанию
  - Дневной (dailyHoroscope) - каждый день в 6:00 МСК
  - Недельный (weeklyHoroscope) - раз в неделю
  - Месячный (monthlyHoroscope) - раз в месяц

- ✅ **Синастрии** - кэшируются по партнеру
  - Краткий режим (brief) - бесплатно для всех
  - Полный режим (full) - только премиум

### 2. Автоматическое обновление гороскопов

#### Дневной гороскоп:
- ⏰ Обновляется **каждый день в 6:00 утра по МСК**
- 🔄 Автоматически проверяется при входе пользователя
- 💾 Новый гороскоп сохраняется в БД

#### Недельный гороскоп:
- ⏰ Обновляется раз в неделю (7 дней)
- 🔄 Проверяется автоматически

#### Месячный гороскоп:
- ⏰ Обновляется раз в месяц (30 дней)
- 🔄 Проверяется автоматически

### 3. Регенерация текстов (пересказ другими словами)

#### Доступность:
- 🔒 Только для **премиум пользователей**
- 🆓 **Первая регенерация в день** - бесплатно
- ⭐ **Последующие регенерации** - 50 звезд Telegram

#### Что можно регенерировать:
- Three Keys (три ключа)
- Natal Summary (краткое описание карты)
- Full Natal (полный разбор карты)
- Синастрии

#### Как работает:
1. Пользователь нажимает кнопку "Рассказать по-другому"
2. Система проверяет лимиты
3. Если первая регенерация сегодня - бесплатно
4. Если не первая - списывает 50 звезд
5. Генерирует новый текст через OpenAI
6. Сохраняет в БД
7. Показывает пользователю

#### Защита от повторных запросов:
- ✅ Все генерации сохраняются в БД
- ✅ При повторном открытии - берется из кэша
- ✅ Нет повторных запросов к OpenAI API
- ✅ Экономия токенов и денег

### 4. Логика работы

#### При первом входе:
```
1. Пользователь вводит данные
2. Рассчитывается натальная карта (Swiss Ephemeris)
3. Генерируются ВСЕ тексты сразу:
   - Three Keys
   - Гороскопы (день, неделя, месяц)
   - Deep Dive анализы (5 разделов)
4. Все сохраняется в БД
5. Пользователь видит результат
```

#### При повторном входе:
```
1. Загружаются данные из БД
2. Проверяются временные метки
3. Если гороскопы устарели - обновляются автоматически
4. Если актуальны - показываются из кэша
5. НЕТ повторных запросов к OpenAI
```

#### При регенерации:
```
1. Проверка: премиум ли пользователь?
2. Проверка: первая регенерация сегодня?
3. Если да - бесплатно
4. Если нет - проверка баланса звезд
5. Списание 50 звезд
6. Генерация нового текста
7. Сохранение в БД
8. Обновление UI
```

## Файлы системы

### Основные сервисы:
- `services/contentGenerationService.ts` - управление генерациями и кэшем
- `services/storageService.ts` - работа с БД
- `services/astrologyService.ts` - астрологические расчеты

### API endpoints:
- `pages/api/astrology/regenerate.ts` - регенерация текстов

### Компоненты:
- `components/RegenerateButton.tsx` - кнопка регенерации

## Безопасность

✅ Все генерации привязаны к userId
✅ Проверка премиум статуса
✅ Защита от множественных запросов
✅ Автоматический возврат звезд при ошибке
✅ Логирование всех операций
