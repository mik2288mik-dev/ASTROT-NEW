# 📚 Пошаговая инструкция: Настройка DATABASE_URL в Railway

## Что такое DATABASE_URL?

`DATABASE_URL` - это строка подключения к вашей базе данных PostgreSQL в Railway. Она выглядит примерно так:

```
postgresql://postgres:пароль@хост:5432/название_базы
```

## ⚠️ Важно: Публичный vs Внутренний URL

Railway предоставляет **два типа** URL для базы данных:

1. **Внутренний URL** (например: `postgres.railway.internal`) 
   - Работает только внутри сети Railway
   - Может не работать в Docker контейнерах
   - ❌ **НЕ используйте этот URL**

2. **Публичный URL** (например: `containers-us-west-123.railway.app`)
   - Работает откуда угодно
   - Подходит для миграций и подключений
   - ✅ **Используйте этот URL**

## 💻 Как используется DATABASE_URL в коде

В проекте `DATABASE_URL` читается из переменных окружения через `process.env.DATABASE_URL`:

```typescript
// Пример из lib/db.ts
import { Pool, Client } from 'pg';

// Чтение DATABASE_URL из переменных окружения
const client = new Client({
  connectionString: process.env.DATABASE_URL, // Чтение переменной из окружения
});

client.connect()
  .then(() => console.log('Connected to database'))
  .catch(err => console.error('Database connection error:', err.stack));
```

**Важно:** Код автоматически читает `DATABASE_URL` из переменных окружения. Вам нужно только установить его в Railway Variables.

## 📝 Пошаговая инструкция

### Шаг 1: Откройте Railway Dashboard

1. Зайдите на https://railway.app
2. Войдите в свой аккаунт
3. Выберите ваш проект

### Шаг 2: Найдите вашу базу данных

1. В левом меню найдите раздел **"Database"** или **"Postgres"**
2. Кликните на вашу базу данных

### Шаг 3: Получите публичный DATABASE_URL

**Вариант A: Через вкладку "Connect"**

1. В открывшемся окне базы данных найдите вкладку **"Connect"** или **"Connection"**
2. Найдите раздел **"Public Networking"** или **"Public URL"**
3. Скопируйте строку подключения (она начинается с `postgresql://`)
4. Это и есть ваш публичный DATABASE_URL

**Вариант B: Через вкладку "Variables"**

1. В Railway Dashboard выберите ваш проект (не базу данных, а сам проект приложения)
2. Перейдите в раздел **"Variables"** или **"Environment Variables"**
3. Найдите переменную `DATABASE_URL`
4. Если она содержит `railway.internal` - это внутренний URL, нужно заменить на публичный

**Вариант C: Через настройки базы данных**

1. Откройте вашу базу данных в Railway
2. Перейдите в **"Settings"** или **"Configuration"**
3. Найдите раздел **"Public Networking"**
4. Включите публичный доступ (если выключен)
5. Скопируйте публичный connection string

### Шаг 4: Установите DATABASE_URL в переменные окружения проекта

1. В Railway Dashboard выберите ваш **проект приложения** (не базу данных)
2. Перейдите в раздел **"Variables"** или **"Environment Variables"**
3. Найдите переменную `DATABASE_URL` или создайте новую:
   - Нажмите **"New Variable"** или **"Add Variable"**
   - Имя: `DATABASE_URL`
   - Значение: вставьте скопированный публичный URL
4. Сохраните изменения

### Шаг 5: Проверьте формат DATABASE_URL

Убедитесь, что ваш DATABASE_URL выглядит примерно так:

```
postgresql://postgres:ваш_пароль@containers-us-west-123.railway.app:5432/railway
```

**Проверьте:**
- ✅ Начинается с `postgresql://`
- ✅ Хост содержит `containers-` или похожий публичный домен
- ✅ НЕ содержит `railway.internal`
- ✅ Содержит порт (обычно `:5432`)
- ✅ Содержит название базы данных в конце

### Шаг 6: Проверьте, что переменная установлена

1. В Railway Dashboard → ваш проект → Variables
2. Убедитесь, что `DATABASE_URL` присутствует в списке
3. Значение должно быть публичным URL (не внутренним)

## 🔍 Как отличить публичный URL от внутреннего?

**Внутренний URL (НЕ используйте):**
```
postgresql://postgres:пароль@postgres.railway.internal:5432/railway
```
- Содержит `railway.internal`
- Может не работать для миграций

**Публичный URL (используйте этот):**
```
postgresql://postgres:пароль@containers-us-west-123.railway.app:5432/railway
```
- Содержит `containers-` или похожий домен
- Работает везде

## 🧪 Проверка настройки

После установки DATABASE_URL проверьте:

### Вариант 1: Через Railway CLI

```bash
# Установите Railway CLI если еще не установлен
npm i -g @railway/cli

# Войдите в Railway
railway login

# Выберите проект
railway link

# Проверьте переменные
railway variables
```

### Вариант 2: Через API после деплоя

После деплоя приложения вызовите:

```bash
curl https://ваш-домен.railway.app/api/migrations/run
```

Если миграции успешно запустились - DATABASE_URL настроен правильно!

### Вариант 3: Через логи Railway

1. Railway Dashboard → ваш проект → Deployments
2. Откройте последний деплой
3. Посмотрите логи
4. Ищите строки вида:
   ```
   [Migrations] Connecting to database: containers-us-west-123.railway.app:5432/railway
   ```
5. Если видите `railway.internal` - используйте публичный URL

## ❓ Частые вопросы

### Q: Где именно найти публичный URL?

A: В Railway Dashboard:
1. Откройте вашу базу данных
2. Вкладка "Connect" → раздел "Public Networking"
3. Или Settings → Public Networking

### Q: Что если я не вижу опцию "Public Networking"?

A: 
1. Убедитесь, что вы открыли именно базу данных (Postgres), а не проект
2. Проверьте, что база данных создана и запущена
3. Попробуйте перезапустить базу данных в Railway

### Q: Можно ли использовать внутренний URL?

A: Только если:
- Ваше приложение запускается внутри сети Railway
- Вы уверены, что Docker контейнеры имеют доступ к внутренней сети
- В противном случае используйте публичный URL

### Q: Безопасно ли использовать публичный URL?

A: Да, если:
- URL содержит пароль и доступен только вам
- Вы не публикуете его в публичных репозиториях
- Railway автоматически управляет безопасностью подключения

### Q: Что делать, если миграции все равно не работают?

A:
1. Проверьте логи Railway на наличие ошибок
2. Убедитесь, что DATABASE_URL установлен в Variables проекта (не базы данных)
3. Проверьте формат URL (должен начинаться с `postgresql://`)
4. Убедитесь, что база данных запущена
5. Попробуйте перезапустить деплой

## 📸 Визуальная помощь

Если у вас есть доступ к Railway Dashboard, ищите:

1. **База данных** → **Connect** → **Public Networking** → скопируйте URL
2. **Проект** → **Variables** → добавьте/обновите `DATABASE_URL` → вставьте публичный URL

## ✅ Чеклист

Пройдитесь по этому списку:

- [ ] Открыл Railway Dashboard
- [ ] Нашел свою базу данных (Postgres)
- [ ] Нашел раздел "Public Networking" или "Connect"
- [ ] Скопировал публичный DATABASE_URL (содержит `containers-` или похожий домен)
- [ ] Открыл проект приложения (не базу данных)
- [ ] Перешел в раздел "Variables"
- [ ] Добавил/обновил переменную `DATABASE_URL`
- [ ] Вставил публичный URL в значение переменной
- [ ] Сохранил изменения
- [ ] Проверил, что URL НЕ содержит `railway.internal`
- [ ] Проверил формат URL (начинается с `postgresql://`)

Если все пункты отмечены - DATABASE_URL настроен правильно! 🎉

## 🚀 Что дальше?

После настройки DATABASE_URL:

1. Railway автоматически запустит миграции при следующем деплое
2. Или запустите миграции вручную через API: `/api/migrations/run`
3. Проверьте логи Railway на успешное выполнение миграций
4. Убедитесь, что таблицы созданы в базе данных

---

**Нужна помощь?** Проверьте логи Railway или обратитесь к документации Railway: https://docs.railway.app