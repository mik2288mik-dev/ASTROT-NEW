# 🔄 Схема работы сохранения и загрузки данных

## 📥 Загрузка данных при старте приложения

```
┌─────────────────────────────────────────────────────────────┐
│  ПОЛЬЗОВАТЕЛЬ ОТКРЫВАЕТ ПРИЛОЖЕНИЕ                          │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  App.tsx: useEffect() - загрузка данных                     │
│  1. Получить Telegram ID пользователя                       │
│  2. Вызвать getProfile()                                    │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  storageService.ts: getProfile()                            │
│  GET /api/users/{userId}                                    │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  pages/api/users/[id].ts: GET метод                         │
│  1. db.users.get(userId)                                    │
│  2. Преобразовать snake_case → camelCase ⭐                 │
│  3. Вернуть clientUser                                      │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  lib/db.ts: db.users.get()                                  │
│  SELECT * FROM users WHERE id = $1                          │
│  Возвращает: { is_setup, is_premium, birth_date, ... }     │
└─────────────────────────┬───────────────────────────────────┘
                          │
            ┌─────────────┴─────────────┐
            │                           │
            ▼                           ▼
    ┌──────────────┐          ┌──────────────┐
    │  404 ответ   │          │  200 ответ   │
    │  профиль     │          │  профиль     │
    │  не найден   │          │  найден      │
    └──────┬───────┘          └──────┬───────┘
           │                         │
           ▼                         ▼
    ┌──────────────┐          ┌──────────────┐
    │              │          │  isSetup?    │
    │  ONBOARDING  │          │              │
    │              │          └──────┬───────┘
    └──────────────┘                 │
                         ┌───────────┴───────────┐
                         │                       │
                         ▼                       ▼
                  ┌──────────────┐        ┌──────────────┐
                  │  isSetup:    │        │  isSetup:    │
                  │  false       │        │  true        │
                  └──────┬───────┘        └──────┬───────┘
                         │                       │
                         ▼                       ▼
                  ┌──────────────┐        ┌──────────────┐
                  │              │        │              │
                  │  ONBOARDING  │        │  DASHBOARD   │
                  │              │        │  ✅          │
                  └──────────────┘        └──────────────┘
```

## 📤 Сохранение данных после Onboarding

```
┌─────────────────────────────────────────────────────────────┐
│  ПОЛЬЗОВАТЕЛЬ ЗАПОЛНЯЕТ ФОРМУ ONBOARDING                    │
│  - Имя                                                      │
│  - Дата рождения                                           │
│  - Время рождения                                          │
│  - Место рождения                                          │
│  - [✓] Запомнить данные для будущих входов                │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  Onboarding.tsx: handleNext() (шаг 3)                       │
│  1. Создать профиль                                         │
│  2. isSetup = rememberData (true/false)                     │
│  3. Вызвать onComplete(profile)                             │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  App.tsx: handleOnboardingComplete()                        │
│  1. Добавить Telegram ID и isAdmin                          │
│  2. Если isSetup === true → saveProfile()                   │
│  3. Рассчитать карту                                        │
│  4. Если isSetup === true → saveChartData()                 │
│  5. Перейти на Hook → Dashboard                             │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  storageService.ts: saveProfile()                           │
│  POST /api/users/{userId}                                   │
│  Body: { isSetup, isPremium, birthDate, ... } (camelCase)   │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  pages/api/users/[id].ts: POST метод                        │
│  1. Преобразовать camelCase → snake_case                    │
│  2. db.users.set(userId, dbUser)                            │
│  3. Преобразовать обратно snake_case → camelCase ⭐         │
│  4. Вернуть clientUser                                      │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  lib/db.ts: db.users.set()                                  │
│  INSERT INTO users (...) VALUES (...)                       │
│  ON CONFLICT (id) DO UPDATE ...                             │
│  Сохраняет: { is_setup, is_premium, birth_date, ... }      │
└─────────────────────────────────────────────────────────────┘
```

## 🔑 Ключевые моменты

### 1. Преобразование форматов данных

```
┌─────────────────────────────────────────────────────────────┐
│                  БАЗА ДАННЫХ (PostgreSQL)                   │
│  Формат: snake_case                                         │
│  ├─ is_setup      (boolean)                                 │
│  ├─ is_premium    (boolean)                                 │
│  ├─ birth_date    (text)                                    │
│  ├─ birth_time    (text)                                    │
│  └─ birth_place   (text)                                    │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          │  API: pages/api/users/[id].ts
                          │  ⬇️ GET:  snake_case → camelCase ⭐
                          │  ⬆️ POST: camelCase → snake_case
                          │
┌─────────────────────────┴───────────────────────────────────┐
│                  КЛИЕНТ (React / TypeScript)                │
│  Формат: camelCase                                          │
│  ├─ isSetup       (boolean) ⭐ КЛЮЧЕВОЕ ПОЛЕ                │
│  ├─ isPremium     (boolean)                                 │
│  ├─ birthDate     (string)                                  │
│  ├─ birthTime     (string)                                  │
│  └─ birthPlace    (string)                                  │
└─────────────────────────────────────────────────────────────┘
```

### 2. Логика проверки isSetup

```typescript
// App.tsx, строка 84:

if (storedProfile && storedProfile.isSetup) {
    // ✅ isSetup === true
    // → Профиль сохранен в БД
    // → Пользователь уже проходил Onboarding
    // → Показываем Dashboard
    setView('dashboard');
} else {
    // ❌ isSetup === false или undefined
    // → Профиль не сохранен или не найден
    // → Пользователь новый или не захотел сохранять данные
    // → Показываем Onboarding
    setView('onboarding');
}
```

## 🐛 Что было не так (до исправления)

```
┌─────────────────────────────────────────────────────────────┐
│  БД возвращает: { is_setup: true }                          │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  API (БЫЛ БАГ): Возвращает БЕЗ преобразования              │
│  response.json(user)                                        │
│  → { is_setup: true } ❌                                     │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  Клиент получает: { is_setup: true }                        │
│  Но проверяет:    storedProfile.isSetup                     │
│  Результат:       undefined ❌                               │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  if (storedProfile && storedProfile.isSetup)                │
│  → false (потому что isSetup === undefined) ❌               │
│  → setView('onboarding') ❌                                  │
│  → Существующий пользователь видит Onboarding снова! 🐛     │
└─────────────────────────────────────────────────────────────┘
```

## ✅ Что исправлено

```
┌─────────────────────────────────────────────────────────────┐
│  БД возвращает: { is_setup: true }                          │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  API (ИСПРАВЛЕНО): Преобразует формат ⭐                    │
│  const clientUser = {                                       │
│    isSetup: user.is_setup,  // ✅ преобразование            │
│    isPremium: user.is_premium,                              │
│    birthDate: user.birth_date,                              │
│    ...                                                      │
│  };                                                         │
│  response.json(clientUser)                                  │
│  → { isSetup: true } ✅                                      │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  Клиент получает: { isSetup: true } ✅                      │
│  Проверяет:       storedProfile.isSetup                     │
│  Результат:       true ✅                                    │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│  if (storedProfile && storedProfile.isSetup)                │
│  → true (потому что isSetup === true) ✅                     │
│  → setView('dashboard') ✅                                   │
│  → Существующий пользователь видит Dashboard! 🎉            │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Сравнение: До и После

| Параметр | До исправления | После исправления |
|----------|----------------|-------------------|
| API GET возвращает | `{ is_setup: true }` | `{ isSetup: true }` ✅ |
| Клиент проверяет | `storedProfile.isSetup` | `storedProfile.isSetup` |
| Значение | `undefined` ❌ | `true` ✅ |
| Результат проверки | `false` | `true` ✅ |
| Отображаемая страница | Onboarding ❌ | Dashboard ✅ |
| Пользовательский опыт | Плохой 😞 | Отличный 😊 |

---

**Итог:** Одна строка кода исправила критическую проблему UX! 🚀
