import fs from 'node:fs';

function replaceOnce(file, before, after) {
  const source = fs.readFileSync(file, 'utf8');
  if (source.includes(after) && !source.includes(before)) return false;
  const count = source.split(before).length - 1;
  if (count === 0 && source.includes(after)) return false;
  if (count !== 1) throw new Error(`${file}: expected one anchor, got ${count}`);
  fs.writeFileSync(file, source.replace(before, after));
  return true;
}

const changed = [];
const edit = (file, before, after, label = file) => {
  if (replaceOnce(file, before, after)) changed.push(label);
};

edit('lib/personalForecastCache.ts', "} from './personalForecastGeneration';", "} from './personalForecastSimpleGeneration';", 'cache-runtime');
edit('pages/api/content/forecast/personal.ts', "import { getPersonalForecastGenerationDiagnosticCode } from '../../../../lib/personalForecastGeneration';", "import { getPersonalForecastGenerationDiagnosticCode } from '../../../../lib/personalForecastSimpleGeneration';", 'route-runtime');
edit('lib/personalForecastContract.ts', "  'personal-forecast-feed.v48-nebo-human-voice',", "  'personal-forecast-feed.v49-simple-human-horoscope',", 'prompt-version');
edit('lib/personalForecastContract.ts', "export const PERSONAL_FORECAST_CACHE_VERSION = 'personal-forecast-cache-v20-nebo-human-voice';", "export const PERSONAL_FORECAST_CACHE_VERSION = 'personal-forecast-cache-v21-simple-human-horoscope';", 'cache-version');
edit('lib/personalForecastContract.ts', "export const PERSONAL_FORECAST_CALCULATION_VERSION = 'personal-forecast-luna-raw-profile-brief-v12';", "export const PERSONAL_FORECAST_CALCULATION_VERSION = 'personal-forecast-luna-raw-profile-v13';", 'calculation-version');
edit('lib/personalForecastContract.ts', "export const PERSONAL_FORECAST_CONTRACT_VERSION = 'personal-forecast-feed-v30-nebo-human-voice';", "export const PERSONAL_FORECAST_CONTRACT_VERSION = 'personal-forecast-feed-v31-simple-human-horoscope';", 'contract-version');
edit('lib/personalForecastContract.ts', "    || !(['overview', 'dynamic'] as const).includes(\n      section.kind as 'overview' | 'dynamic',\n    )", "    || !(['overview', 'fixed', 'dynamic'] as const).includes(\n      section.kind as 'overview' | 'fixed' | 'dynamic',\n    )", 'section-kinds');
edit('lib/personalForecastContract.ts', `function canonicalSectionIdentityValid(\n  section: ForecastSection,\n  _period: PersonalForecastPeriod,\n): boolean {\n  if (section.kind === 'dynamic') {\n    return (\n      section.id.startsWith('semantic:')\n      && (section.title === undefined || isSimpleDynamicTitle(section.title))\n      && section.fixedKey === undefined\n      && section.sourceTopicKey === undefined\n    );\n  }\n  return false;\n}`, `function canonicalSectionIdentityValid(\n  section: ForecastSection,\n  _period: PersonalForecastPeriod,\n): boolean {\n  if (section.kind === 'fixed') {\n    return (\n      typeof section.fixedKey === 'string'\n      && FIXED_FORECAST_SECTION_KEYS.includes(section.fixedKey)\n      && section.id === \`fixed:\${section.fixedKey}\`\n      && section.sourceTopicKey === section.fixedKey\n      && typeof section.title === 'string'\n      && Boolean(section.title.trim())\n    );\n  }\n  if (section.kind === 'dynamic') {\n    return (\n      section.id.startsWith('semantic:')\n      && (section.title === undefined || isSimpleDynamicTitle(section.title))\n      && section.fixedKey === undefined\n      && section.sourceTopicKey === undefined\n    );\n  }\n  return false;\n}`, 'canonical-fixed-sections');
edit('lib/personalForecastContract.ts', `function personalForecastAstrologerBriefValid(value: unknown): value is PersonalForecastAstrologerBrief {\n  if (!value || typeof value !== 'object' || Array.isArray(value)) return false;\n  const basis = value as PersonalForecastAstrologerBrief;\n  return (basis.tone === 'favorable' || basis.tone === 'mixed' || basis.tone === 'demanding')\n    && Array.isArray(basis.observations)\n    && basis.observations.length >= 2 && basis.observations.length <= 4\n    && basis.observations.every((observation) => {\n      if (typeof observation !== 'string') return false;\n      const words = observation.trim().split(/\\s+/u).filter(Boolean).length;\n      return words >= 8 && words <= 45;\n    })\n    && new Set(basis.observations.map((observation) => observation.trim().toLowerCase())).size === basis.observations.length\n    && typeof basis.briefSignature === 'string' && Boolean(basis.briefSignature.trim());\n}`, `function personalForecastAstrologerBriefValid(value: unknown): value is PersonalForecastAstrologerBrief {\n  if (!value || typeof value !== 'object' || Array.isArray(value)) return false;\n  const basis = value as PersonalForecastAstrologerBrief;\n  return (basis.tone === 'favorable' || basis.tone === 'mixed' || basis.tone === 'demanding')\n    && Array.isArray(basis.observations)\n    && basis.observations.length >= 1 && basis.observations.length <= 4\n    && basis.observations.every((observation) => typeof observation === 'string' && Boolean(observation.trim()) && observation.length <= 1200)\n    && typeof basis.briefSignature === 'string' && Boolean(basis.briefSignature.trim());\n}`, 'brief-metadata-no-word-quotas');
edit('lib/personalForecastContract.ts', "  const expectedSectionCount = forecast.sections.length === 1;", "  const expectedSectionCount = forecast.sections.length === 4;", 'four-human-sections');
edit('lib/personalForecastWireCompatibility.ts', "  const closing = original.sections[0];", "  const closing = original.sections.find((section) => section.contentBlocks.some((block) => block.role === 'action')) || original.sections.at(-1)!;", 'legacy-closing');
edit('lib/personalForecastWireCompatibility.ts', `    } else {\n      // Current Day generation guarantees 3–4 sentences. Keep every sentence\n      // exactly once and in order; the last fragment may contain two sentences.\n      const sentences = original.overview.text.trim().split(/(?<=[.!?…][»”\"')\\]]*)\\s+/u);\n      if (sentences.length < 3) throw new Error('PERSONAL_FORECAST_LEGACY_STRUCTURE_UNSUPPORTED');\n      overview = legacySection(original.overview, sentences[0], 'overview', 100);\n      sections = [\n        legacySection(original.overview, sentences[1], 'semantic:legacy-body:1', 90),\n        legacySection(original.overview, sentences.slice(2).join(' '), 'semantic:legacy-body:2', 70),\n        legacySection(closing, closing.text, closing.id, 80),\n      ];`, `    } else {\n      // New packages have three readable topic sections plus a closing. Project\n      // those already-authorized strings into the old four-part wire shape.\n      const readable = original.sections.filter((section) => section.id !== closing.id);\n      const first = readable[0] || original.overview;\n      const second = readable[1] || readable[0] || original.overview;\n      overview = legacySection(original.overview, original.overview.text, 'overview', 100);\n      sections = [\n        legacySection(first, first.text, 'semantic:legacy-body:1', 90),\n        legacySection(second, second.text, 'semantic:legacy-body:2', 70),\n        legacySection(closing, closing.text, closing.id, 80),\n      ];`, 'legacy-premium-day');

console.log(JSON.stringify({ changed }));
