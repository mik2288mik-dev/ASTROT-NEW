name: Validate NEBO parity pass 5
on:
  push:
    branches: [nebo-parity-pass5-20260908]
permissions:
  contents: write
concurrency:
  group: nebo-parity-pass5
  cancel-in-progress: true
jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      NEXT_TELEMETRY_DISABLED: '1'
      NODE_OPTIONS: --max-old-space-size=6144
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: node scripts/nebo-pass5-integrate.mjs
      - run: npm ci --no-audit --no-fund
      - name: Lint compatibility adapter
        run: npx eslint components/nebo-v2/NeboUnionRoom.tsx components/nebo-v2/EntryPoints.tsx App.tsx pages/_app.tsx --max-warnings=0
      - name: TypeScript
        run: npx tsc --noEmit
      - name: Compatibility and access tests
        run: npx jest --runInBand __tests__/synastry-client-delivery.test.ts __tests__/forecast-synastry-access-integration.test.ts __tests__/compatibility-editorial-layout.test.ts
      - name: Production build
        env:
          NODE_ENV: production
        run: npm run build
      - name: Diff hygiene
        run: |
          git diff --check
          git diff --name-only origin/main...HEAD
          git diff --name-only | sort
      - name: Commit validated integration
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'NEBO validation'
          git config user.email 'nebo-validation@users.noreply.github.com'
          git add App.tsx pages/_app.tsx
          if ! git diff --cached --quiet; then
            git commit -m 'feat(ui): integrate native NEBO compatibility flow'
            git push origin HEAD:refs/heads/nebo-parity-pass5-20260908
          fi
